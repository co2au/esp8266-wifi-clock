# Normalize text files to LF on checkout/commit (cross-platform safety)
*          text=auto eol=lf

# Treat Arduino .ino files as C++ for diffs/highlighting
*.ino      linguist-language=C++

# Explicitly text (UTF-8 by convention)
*.{ino,cpp,h,hpp,c,S}   text
*.{sh,bash}             text
*.{md,txt}              text
*.{ini,cfg,conf,properties} text
*.{json,yml,yaml,xml,csv}   text
Makefile                text

# Binary artifacts (no diff)
*.{bin,elf,hex}         -text
*.{jpg,jpeg,png,gif,bmp,ico} -text
*.{pdf,zip,gz,bz2,7z}   -text

# Optional: ignore export of build/ when making archives
build/                  export-ignore

